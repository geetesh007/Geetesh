<div class="content-inner">

	{{#each announcements}}
	<div class="announcement-banner badge-background" data-id="{{id}}">
		<div>
			<p class="subtitle secondary">On {{readableDate}}, The Creator said...</p>
			<p class="info">{{message}}</p>
		</div>
		{{#if dismissable}}
		<div>
			<div class="remove-announcement">
				<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="feather feather-x-circle"><circle cx="12" cy="12" r="10"></circle><line x1="15" y1="9" x2="9" y2="15"></line><line x1="9" y1="9" x2="15" y2="15"></line></svg>
			</div>
		</div>
		{{/if}}
	</div>
	{{/each}}
  <img src="https://cdn.discordapp.com/attachments/887560969599987733/891948508964352020/download.png" style="border-style: none; width:50px;">
	<h1 class="title">Welcome Buddy!</h1>
	<p>To HealManga! Read Thousands Of Managa Online And For Anime Visit <a href="https://healdb.me" target="_blank" class="anchor">HealAnime.</a></p>

	{{#if reading}}
	<section class="section continue-reading" data-list-id="continue-reading" data-list-type="list">
		<h2 class="sub-title spread do-spread"><span>🚒Trending On HealManga</span> {{> list-type }}</h2>
		<div class="manga-grid show-all-wrapper do-show-all">
			
			{{#each reading}}
			{{> manga-card manga=this toChapter=true}}
			{{/each}}

			<button class="show-all">Show <span class="more-count"></span> more</button>

		</div>
	</section>
	{{/if}}
	{{#each lists}}
	{{> list list=this}}
	{{/each}}
	{{#if popular}}
	<section class="section popular" data-list-id="popular" data-list-type="list">
		<h2 class="sub-title spread do-spread"><span>🔥Popular right now</span> {{> list-type }}</h2>
		<div class="manga-grid">
		{{#each popular}}
			{{>manga-card manga=this}}
		{{/each}}
		</div>
	</section>
	{{/if}}

</div>

<script src="/js/pages/home.js"></script>
<script src="/js/reusable/lists.js"></script>
<script src="/js/reusable/list-type.js"></script>